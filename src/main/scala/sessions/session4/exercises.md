
📝 **Exercise 1**:

Create a `Rectangle` class with:
1. two attributes `width` and `height`
2. a method `area` that computes the area
3. a method `perimeter` that computes the perimeter

Then create an object and print the area and the perimeter

--- 
📝 **Exercise 2**:

Create a class hierarchy where:
1. A `Vehicle` class has a `move()` method that prints `"The vehicle is moving"`
2. Two subclasses: `Car` and `Bike` override `move()`
3. In a main program, store several vehicles in a list and call `move()` on each one
