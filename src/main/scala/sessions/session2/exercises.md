
📝 **Exercise 1**:

Write a program that:

1. Generates a list of words (e.g. `List("A","B","A","C","B","A")`)
2. Computes the number of occurrences of each word
3. Stores the result in a `Map[String, Int]`


--- 
📝 **Exercise 2**: `List` vs `Vector` comparison

Create a list and a vector of 100,000 elements
- Measure the concatenation time:
  - On the left (`0 +: collection`)
  - On the right (`collection :+ 0`)
- Compare performance


